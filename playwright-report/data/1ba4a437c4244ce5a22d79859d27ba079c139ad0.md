# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: goals.spec.ts >> Goals >> create weekly volume goal
- Location: tests/e2e/goals.spec.ts:14:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('20km')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByText('20km')

```

```yaml
- banner: Running Coach
- main:
  - heading "목표" [level=1]
  - heading "활성 목표" [level=2]
  - paragraph: 주간 20.00km 목표
  - paragraph: "이번 주: 5.00km / 20.00km (25%)"
  - button "수정"
  - button "완료"
  - button "포기"
- navigation:
  - link "🏠 홈":
    - /url: /
  - link "📋 기록":
    - /url: /runs
  - link "⚡ 코치":
    - /url: /coach
  - link "⚙️ 설정":
    - /url: /settings
```

# Test source

```ts
  1  | import { expect, test } from '@playwright/test'
  2  | import { AUTH_FILE } from './shared'
  3  | 
  4  | test.use({ storageState: AUTH_FILE })
  5  | 
  6  | test.describe('Goals', () => {
  7  |   test.describe.configure({ mode: 'serial' })
  8  | 
  9  |   test('goals page loads', async ({ page }) => {
  10 |     await page.goto('/goals')
  11 |     await expect(page.getByRole('heading', { name: '목표' })).toBeVisible()
  12 |   })
  13 | 
  14 |   test('create weekly volume goal', async ({ page }) => {
  15 |     await page.goto('/goals')
  16 |     await page.getByRole('button', { name: '새 목표 만들기' }).click()
  17 | 
  18 |     await page.getByRole('button', { name: '주간 볼륨' }).click()
  19 |     await page.locator('[name="weekly_km_target"]').fill('20')
  20 |     await page.getByRole('button', { name: '저장' }).click()
  21 | 
> 22 |     await expect(page.getByText('20km')).toBeVisible()
     |                                          ^ Error: expect(locator).toBeVisible() failed
  23 |   })
  24 | 
  25 |   test('home dashboard shows ring after goal created', async ({ page }) => {
  26 |     await page.goto('/')
  27 |     const circles = await page.locator('svg circle').count()
  28 |     expect(circles).toBeGreaterThan(0)
  29 |   })
  30 | 
  31 |   test('abandon active goal → moves to completed list', async ({ page }) => {
  32 |     await page.goto('/goals')
  33 |     page.on('dialog', (d) => d.accept())
  34 |     await page.getByRole('button', { name: '포기' }).click()
  35 |     await expect(page.getByText('완료된 목표')).toBeVisible()
  36 |   })
  37 | })
  38 | 
```